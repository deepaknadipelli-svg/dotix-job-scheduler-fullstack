# Dotix Job Scheduler (Full Stack)

## Backend
cd backend
npm install
npm start

## Frontend
cd frontend
npm install
npm run dev

## Database
Create database `dotix_jobs` and `jobs` table before running backend.
